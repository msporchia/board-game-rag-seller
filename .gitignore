# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.venv/
venv/
.env

# Enrichment store (dati locali, non versionati)
data/
*.db

# IDE
.idea/
.vscode/

# Local-only proprietary catalog (served by the mock; never committed)
mock/catalog.json

# Local-only compose override (machine-specific, e.g. reusing existing volumes)
docker-compose.override.yml
