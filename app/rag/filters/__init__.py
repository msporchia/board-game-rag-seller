"""Structured search filters (Phase 3, hybrid search). Import what you need:

- `search_filters.SearchFilters` (+ its name → class `REGISTRY`)
- `rerank.rerank_soft` / `rerank.SOFT_BOOST`
- `errors.UnknownFilterError`

The individual filters live one-per-file under set/ range/ bool/ (see `filter.py` for the base).
"""
